@model EoS.Models.Shared.Outcome

@{
    ViewBag.Title = "Delete Outcome";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>OutCome @Model.OutcomeName</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.OutcomeName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.OutcomeName)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-danger" />&nbsp;&nbsp;
            @Html.ActionLink("Back to Outcome list", "Index")
        </div>
    }
</div>
