@model IEnumerable<EoS.Models.Shared.FundingPhase>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FundingPhaseName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FundingPhaseName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FundingPhaseID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FundingPhaseID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FundingPhaseID })
        </td>
    </tr>
}

</table>
