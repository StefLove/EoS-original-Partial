@model IEnumerable<EoS.Models.Shared.InnovationLevel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.InnovationLevelName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.InnovationLevelName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.InnovationLevelID }) |
            @Html.ActionLink("Details", "Details", new { id=item.InnovationLevelID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.InnovationLevelID })
        </td>
    </tr>
}

</table>
