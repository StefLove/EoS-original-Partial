@model IEnumerable<EoS.Models.Shared.FundingAmount>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FundingAmountValue)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FundingAmountValue)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FundingAmountID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FundingAmountID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FundingAmountID })
        </td>
    </tr>
}

</table>
