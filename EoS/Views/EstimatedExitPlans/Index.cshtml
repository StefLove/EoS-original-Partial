@model IEnumerable<EoS.Models.Shared.EstimatedExitPlan>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EstimatedExitPlanName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EstimatedExitPlanName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EstimatedExitPlanID }) |
            @Html.ActionLink("Details", "Details", new { id=item.EstimatedExitPlanID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EstimatedExitPlanID })
        </td>
    </tr>
}

</table>
