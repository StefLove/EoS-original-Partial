@model IEnumerable<EoS.Models.Shared.ProjectDomain>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProjectDomainName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProjectDomainName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProjectDomainID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProjectDomainID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProjectDomainID })
        </td>
    </tr>
}

</table>
