@model IEnumerable<EoS.Models.IdeaCarrier.FundingDivision>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FundingDivisionName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FundingDivisionName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FundingDivisionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FundingDivisionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FundingDivisionID })
        </td>
    </tr>
}

</table>
