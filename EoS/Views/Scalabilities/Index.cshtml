@model IEnumerable<EoS.Models.Shared.Scalability>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ScalabilityName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ScalabilityName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ScalabilityID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ScalabilityID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ScalabilityID })
        </td>
    </tr>
}

</table>
